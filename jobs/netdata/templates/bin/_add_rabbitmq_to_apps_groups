#!/usr/bin/env bash

set -e

[ -d "${NETDATA_CONF_DIR:?must be set}" ] || (
  echo "$NETDATA_CONF_DIR doesn't exist - did netdata install correctly?"
  exit 1
)

grep rabbitmq "$NETDATA_CONF_DIR/apps_groups.conf" >/dev/null ||
  echo 'rabbitmq: *rabbitmq*' >> "$NETDATA_CONF_DIR/apps_groups.conf"
