#!/usr/bin/env bash

set -e

mkdir -p "${RABBITMQ_MNESIA_BASE:?must be set}" "${RABBITMQ_MNESIA_BASE%/*}"
chown -fR "${SERVICE_USER:?must be set}":"${SERVICE_GROUP:?must be set}" "$RABBITMQ_MNESIA_BASE" "${RABBITMQ_MNESIA_BASE%/*}"
